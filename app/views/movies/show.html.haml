%h2= "#{@feed.title} animation"
#feed{style:"text-align:center;"}
  - if @movie.available?
    .entry
      .title{style:"padding:3px;"}
        %b= "#{@movie.title} for"
        = render 'picker', date: @movie.event_at, duration: @movie.duration
      = render @movie
  - else
    = render 'generating', movie: @movie