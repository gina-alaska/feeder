$('#image-chooser').html('<%= escape_javascript(render 'image_chooser.html', entries: entries) %>');
<% if params[:select] == 'first' %>
  $('#image-chooser .entry-preview:first a').click()  
<% else %>
  $('#image-chooser .entry-preview:last a').click()    
<% end %>