.row-fluid
  .span2
    = render 'stats'
  .span10
    %h2
      = "Current jobs in #{params[:id].humanize} queue"
      .pull-right
        = link_to admin_queues_path, class: 'btn' do
          %i.icon-arrow-left
          Back
    = render 'job_list', jobs: @queue, name: params[:id].humanize
